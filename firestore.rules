rules_version = '2';

// Firestore is not used by this app — deny all access.
// If you add Firestore later, scope rules per-user like the Realtime Database rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
